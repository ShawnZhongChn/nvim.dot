## 1. Prerequisites

- [x] 1.1 Verify Deno is installed (`deno --version`) ⚠️ Deno not installed - must install before use

## 2. Plugin Installation

- [x] 2.1 Add peek.nvim to lazy.nvim plugin spec with build command
- [x] 2.2 Configure PeekOpen and PeekClose user commands in setup
- [ ] 2.3 Run `:Lazy sync` to install the plugin

## 3. Configuration

- [x] 3.1 Configure peek.nvim with default settings (auto_load, dark theme, webview app)
- [ ] 3.2 Verify Deno build task runs successfully (`deno task --quiet build:fast`)
